---
- name: Deploy all supplementary services
  hosts: localhost
  become: yes
  become_user: root

  tasks:
    - name: Deploy Vault
      include_role: 
        name: vault
      
    - name: Deploy postgres
      include_role: 
        name: postgres

    